# Cobot-WifiPresenceTracker

A very simple presence tracker for Cobot using nmap. This code is neither pretty nor unit-tested but works :-)

## Configuration

`composer install` and adjust `config.sample.php`